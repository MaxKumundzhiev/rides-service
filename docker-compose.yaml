version: '3.4'

services:
    nginx:
        image: nginx:latest
        container_name: my-nginx
        volumes:
            - ./nginx/nginx.conf:/etc/nginx/nginx.conf
        ports:
            - 8000:8000
            - 443:443